import React, { FC } from "react";
import { AuthForm } from "../../../../components";
import { Container } from "./Container";

export const SignUpForm: FC = () => <Container component={AuthForm} />;
