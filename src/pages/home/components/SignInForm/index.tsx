import React, { FC } from "react";
import { AuthForm } from "../../../../components";
import { Container } from "./Container";

export const SignInForm: FC = () => <Container component={AuthForm} />;
